from pathlib import Path  # Import Path from pathlib for managing file system paths

# Define the path to the main configuration file
CONFIG_FILE_PATH = Path("config/config.yaml")

# Define the path to the parameters file, often used for hyperparameters and model settings
PARAMS_FILE_PATH = Path("params.yaml")
